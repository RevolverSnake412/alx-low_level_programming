#include "main.h"
#include <string.h>
/**
 * _strcat - strcat
 *
 * Return: dest
*/
char _strcat(char *dest, char *src)
{	int i = 0, j = 0;
	while (dest[j] = '/0')
	{
		i++;
	}
	while (src[j] = '/0')
	{
		dest[i] = src [j];
		i++;
		j++;
	}
	dest[i] = '/0';
}
