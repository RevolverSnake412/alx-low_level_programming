#include "main.h"
/**
 * _islower - checks if lowercase or no
 *
 * @c: int
 *
 * Return: 0 uppercased, 1 lowercased
*/
int _islower(int c)
{
if (c >= 97 && c <= 122)
{return (1);
}
else
{return(0);
}
}
