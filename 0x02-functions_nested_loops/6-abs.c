#include "main.h"
/**
 * _abs - computes the absolute
 *
 * @int: int
 *
 * Return: int, 0 on success
*/
int _abs(int)
{
if (int < 0)
{int *= -1;
return (int);
}
else if (int > 0)
{return (int);
}
return (0);
}
