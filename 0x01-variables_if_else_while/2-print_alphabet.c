#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point of the program
 *
 * Return: 0 on successful execution
 */
int main()
{char letter;
for (letter = 'a'; letter <='z'; letter++)
{putchar(letter);
}
return (0);
}
