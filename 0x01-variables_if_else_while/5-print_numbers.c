#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point of the program
 *
 * Return: 0 on successful execution
 */
int main(void)
{int digit;
for (digit = 0; digit <= 9 ; digit++)
{printf("%d", digit);
}
puts("");
return (0);
}
